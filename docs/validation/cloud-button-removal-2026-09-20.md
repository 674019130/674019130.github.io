Removed the visible background playback button at the user's request. The previous component is archived with the removal reason. Background video, OS reduced-motion support, and background-tab suspension remain. In-page manual pause/resume is no longer available.

Validation: typecheck passed; 12 tests passed; Node 24 production build passed; home/about/archive output contains the background but no playback button; Safari visual check confirms homepage layout and background remain intact. ESLint was attempted but remains unavailable because the project has no eslint.config file.
